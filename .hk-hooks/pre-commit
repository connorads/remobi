#!/bin/sh
# Run the repository toolchain without installing tools during a commit.
if [ "${HK:-1}" = "0" ]; then
  exit 0
fi
export MISE_EXEC_AUTO_INSTALL=false MISE_ACTIVATE_AGGRESSIVE=true
exec mise exec -- hk run pre-commit -q "$@"
